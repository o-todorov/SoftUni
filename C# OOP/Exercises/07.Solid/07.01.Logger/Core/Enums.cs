namespace Solid.Core
{
    // Error levels: Info > Warning > Error > Critical > Fatal
    enum ReportLevel
    {
        Info        = 0,
        Warning     = 1,
        Error       = 2,
        Critical    = 3,
        Fatal       = 4
    }
}
