let serverUrl = 'http://localhost:3030';
let movieBaseUrl = `${serverUrl}/data/movies`;
let likesBaseUrl = `${serverUrl}/data/likes`;
let loginUrl = `${serverUrl}/users/login`;
let registerUrl = `${serverUrl}/users/register`;
let logoutUrl = `${serverUrl}/users/logout`;
export {serverUrl, 
        movieBaseUrl, 
        likesBaseUrl, 
        loginUrl,
        registerUrl,
        logoutUrl,
};